var chocolateBars = ["snickers", "hundred grand", "kitkat", "skittles" ]

function addElementToBeginningOfArray(elements, element) {
  var new_elements = elements.unshift(element);
  return new_elements;
}